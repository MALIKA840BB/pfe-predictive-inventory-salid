[19:11, 15/06/2026] Malika🌝🌼: PFE Predictive Inventory

Projet de stage basé sur XGBoost et Streamlit.

## Comment exécuter le projet localement :

1. Cloner le dépôt :
   git clone https://github.com/goussaounmalika36-png/pfe-predictive-inventory-salid/new/main

2. Installer les dépendances :
   pip install -r requirements.txt

3. Lancer l'application :

   streamlit run interface.py

Instructions pour l'exécution locale (Ligne de commande)
​Pour exécuter cette application sur votre machine locale via le terminal (CMD) :

1.Ouvrez votre terminal (CMD ou PowerShell) dans le dossier du projet.

 Installez les dépendances nécessaires en exécutant la commande suivante :

pip install -r requirements.txt

 3. *Lancez l'application Streamlit* avec la commande suivante :
   ```bash

streamlit run interface.py

 Une fois la commande exécutée, une nouvelle page s'ouvrira automatiquement dans votre navigateur par défaut avec l'application.
